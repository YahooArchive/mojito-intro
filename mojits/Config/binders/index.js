YUI.add('ConfigBinder', function(Y, NAME)
{
    Y.namespace('mojito.binders')[NAME] =
    {
        init: function(proxy) 
        {
            var self = this;
        }
    };
}, '0.0.1', {requires: ['mojito-client']});
