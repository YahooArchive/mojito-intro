[
    {
        "settings": ["master"],
        "config": {
            "foo": "defaults",
            "bar": "defaults"
        }
    },
    {
        "settings": ["device:iphone"],
        "config": {
            "baz": "defaults.iphone"
        }
    }
]
